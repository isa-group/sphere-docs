import { VersionTable } from "@site/src/components/common";

- **mandatory**
- Field type: `bool`

Setting `hasAnnualPayment` to `true` means that your pricing supports yearly billing.

```yaml
hasAnnualPayment: true
```

<VersionTable name="hasAnnualPayment" libraries={[true, true]} />
