import { VersionTable } from "@site/src/components/common";

- **required**
- Field type: `float`

Price of your addon when billing is set to monthly.

```yaml
addOns:
  postmanFlowsBasic:
    monthlyPrice: 15.00
```

:::info
You have to specify at least `monthlyPrice` or `annualPrice`. A combination
of both also works.
:::

**Libraries support**

<VersionTable name="addOns.<name>.monthlyPrice" libraries={[true, true]} />
