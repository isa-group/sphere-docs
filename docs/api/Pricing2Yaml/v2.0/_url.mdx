import { VersionTable } from "@site/src/components/common";

- **optional**
- Field type: a `str` that represents an `URL` must be begin
  with `http` or `https`.

Field `url` holds a URL that points to your pricing page.

```yaml
url: https://example.org/pricing
```

**Libraries support**

<VersionTable name="url" libraries={[true, true]} />
