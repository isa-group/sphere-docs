import { VersionTable } from "@site/src/components/common";

- **optional**
- Field type: `string`

Specify the version of the pricing. If the field is not provided with a value, the default is `latest`

```yaml
version: "jan-25"
```

**Libraries support**

<VersionTable name="version" libraries={[true, true]} />
