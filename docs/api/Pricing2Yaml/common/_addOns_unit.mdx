import { VersionTable } from "@site/src/components/common";

- **required**
- Field type: `str`

Measure of the addon subscription.

```yaml
addOns:
  gitLFSDataPack:
    unit: user/month
```

**Libraries support**

<VersionTable name="addOns.<name>.unit" libraries={[true, true]} />
