import { VersionTable } from "@site/src/components/common";

- **optional**
- Field type: `str`

Brief summary of what the usage limit restricts.

```yaml
usageLimits:
  useMessagesAccess:
    description: "The number of days you can access message and file information."
```

<VersionTable name="usageLimits.<name>.description" libraries={[true, true]} />
