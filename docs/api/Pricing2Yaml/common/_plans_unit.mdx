import { VersionTable } from "@site/src/components/common";

- **required**
- Field type: `str`

Measure of the plan subscription.

```yaml
plans:
  TEAM:
    unit: user/month
```

**Libraries support**

<VersionTable name="plans.<name>.unit" libraries={[true, true]} />
